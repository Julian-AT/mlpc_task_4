---
phase: 01-project-scaffold-and-data-foundation
plan: 01-01
name: Scaffold repository, dependencies, config, gitignore, and results log
type: standard
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - src/__init__.py
  - src/config.py
  - tests/test_data.py
  - results/log.md
  - report/.gitkeep
  - slides/.gitkeep
  - notebooks/.gitkeep
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
requirements_addressed: [SETUP-01, SETUP-02, SETUP-03, SETUP-04]
---

<objective>
Create the repository skeleton and central configuration that every later Phase 1 task uses.

Output: project directories, dependency documentation, gitignore protections, importable `src` package, `src/config.py`, initial `tests/test_data.py`, and `results/log.md`.

Decision coverage: D-01, D-02, D-04, D-13.
</objective>

<context>
- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/phases/01-project-scaffold-and-data-foundation/01-CONTEXT.md`
- `.planning/phases/01-project-scaffold-and-data-foundation/01-RESEARCH.md`
- `.planning/phases/01-project-scaffold-and-data-foundation/01-VALIDATION.md`
- `.planning/phases/01-project-scaffold-and-data-foundation/01-PATTERNS.md`
- `MLPC_Task4_PRD.md`
- `AGENTS.md`
</context>

<threat_model>
## Threat Model

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01-01 | Course dataset, raw audio, generated `.npz` caches, model binaries, or generated PDFs are accidentally committed. | high | `.gitignore` must include `data/`, audio extensions, generated `.npz` outputs, model artifacts, and generated PDFs. Verify with `git check-ignore`. |
| T-01-02 | `src/config.py` embeds a private absolute dataset path, making the project non-reproducible and leaking local machine details. | medium | Default to `ROOT / "data"` and expose constants only; no user-specific absolute path is committed. |
</threat_model>

<tasks>
  <task id="01-01-01" type="auto">
    <title>Create planned directories and placeholders</title>
    <read_first>
      <file>.planning/phases/01-project-scaffold-and-data-foundation/01-CONTEXT.md</file>
      <file>MLPC_Task4_PRD.md</file>
    </read_first>
    <action>
      Create `src/`, `tests/`, `results/`, `results/figures/`, `report/`, `slides/`, and `notebooks/`. Add `src/__init__.py`. Add `.gitkeep` placeholders under `report/`, `slides/`, and `notebooks/` only if needed to keep empty directories visible.
    </action>
    <verify>
      `test -d src -a -d tests -a -d results -a -d results/figures -a -d report -a -d slides -a -d notebooks`
    </verify>
    <acceptance_criteria>
      <criterion>`src/__init__.py` exists.</criterion>
      <criterion>`results/figures/` exists.</criterion>
      <criterion>No dataset file is added under `data/`.</criterion>
    </acceptance_criteria>
  </task>

  <task id="01-01-02" type="auto">
    <title>Add dependency and ignore policy files</title>
    <read_first>
      <file>.planning/research/STACK.md</file>
      <file>AGENTS.md</file>
      <file>MLPC_Task4_PRD.md</file>
    </read_first>
    <action>
      Create `requirements.txt` with `numpy`, `pandas`, `scikit-learn`, `matplotlib`, `seaborn`, `tqdm`, `mlx`, `librosa`, `soundfile`, `pyarrow`, `joblib`, and `pytest`. Create `.gitignore` entries for Python caches, virtualenvs, `data/`, `*.wav`, `*.flac`, `*.mp3`, generated `results/*.npz`, `results/*.pkl`, `results/*.joblib`, `results/*.json`, `results/*.csv`, generated PDFs, and common local editor/system files. Keep `results/log.md` trackable by adding a negation rule if needed.
    </action>
    <verify>
      `grep -E "numpy|pandas|scikit-learn|pytest" requirements.txt` and `git check-ignore data results/dataset_cache.npz`
    </verify>
    <acceptance_criteria>
      <criterion>`requirements.txt` contains `mlx` and `pytest`.</criterion>
      <criterion>`.gitignore` causes `git check-ignore data` to exit 0.</criterion>
      <criterion>`.gitignore` causes `git check-ignore results/dataset_cache.npz` to exit 0.</criterion>
      <criterion>`results/log.md` is not ignored.</criterion>
    </acceptance_criteria>
  </task>

  <task id="01-01-03" type="auto">
    <title>Create central config and initial validation files</title>
    <read_first>
      <file>MLPC_Task4_PRD.md</file>
      <file>.planning/phases/01-project-scaffold-and-data-foundation/01-RESEARCH.md</file>
      <file>.planning/phases/01-project-scaffold-and-data-foundation/01-VALIDATION.md</file>
    </read_first>
    <action>
      Create `src/config.py` with `ROOT`, `DATA_DIR`, `FEATURES_DIR`, `METADATA_CSV`, `ANNOTATIONS_CSV`, `RESULTS_DIR`, `FIG_DIR`, `SEED = 42`, split fractions `0.70/0.15/0.15`, `ANNOT_BINARIZE_THRESH = 0.5`, `MAJORITY_THRESH = 0.5`, `TEMPORAL_CONTEXT = 2`, `HIGH_AGREEMENT_IOU = 0.6`, the 15-class `CLASS_NAMES` list from the PRD, `NUM_CLASSES`, `LR_GRID`, `MLP_GRID`, `MLP_EPOCHS = 30`, `MLP_BATCH = 1024`, and `MLP_PATIENCE = 5`. Create `tests/test_data.py` with imports skipped or marked TODO until `src/data.py` exists. Create `results/log.md` with a Phase 1 heading and empty verification checklist.
    </action>
    <verify>
      `python -m compileall src` and `python - <<'PY'\nfrom src.config import CLASS_NAMES, NUM_CLASSES, DATA_DIR\nassert NUM_CLASSES == 15\nassert DATA_DIR.name == 'data'\nassert CLASS_NAMES == sorted(CLASS_NAMES)\nPY`
    </verify>
    <acceptance_criteria>
      <criterion>`src/config.py` contains `ANNOT_BINARIZE_THRESH = 0.5`.</criterion>
      <criterion>`src/config.py` contains `MAJORITY_THRESH = 0.5`.</criterion>
      <criterion>`src/config.py` contains `CLASS_NAMES` with 15 entries.</criterion>
      <criterion>`results/log.md` contains `# Results Log` and a Phase 1 section.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `python -m compileall src`
- `python -m pytest` if pytest is installed in the active environment.
- `git check-ignore data results/dataset_cache.npz`
</verification>

<must_haves>
  <truth>D-01: Use `MLPC_Task4_PRD.md` and official assignment docs as Phase 1 source of truth.</truth>
  <truth>D-02: Dataset path defaults to uncommitted local `data/` through `src/config.py`.</truth>
  <truth>D-04: Course dataset files and generated artifacts are ignored by git.</truth>
  <truth>D-13: Phase 1 creates planned directories, dependency file, `.gitignore`, `src/config.py`, `src/data.py`, and `results/log.md`; this plan covers the scaffold subset.</truth>
</must_haves>

<success_criteria>
- Requirements `SETUP-01` through `SETUP-04` are implemented or prepared for later Phase 1 plans.
- The repository can import `src.config`.
- Restricted/generated files are protected by `.gitignore`.
- `results/log.md` exists for later verification notes.
</success_criteria>
