---
phase: 02-splits-preprocessing-metrics-and-baseline
plan: 02-01
name: Implement collector-disjoint splits and class-distribution outputs
status: planned
requirements: [SPLIT-01, SPLIT-02, SPLIT-03]
files: [src/config.py, src/splits.py, tests/test_phase2.py]
---

# Plan 02-01: Collector Splits and Class Distribution

## Objective

Implement collector-disjoint train/validation/test split generation and report-ready class-distribution outputs.

## Tasks

1. Add result path constants for split and distribution artifacts.
2. Implement cache loading, grouped split generation, collector-leakage assertion, split persistence, class-distribution table, and plotting.
3. Add synthetic tests for reproducible disjoint splits and class-distribution counts/rates.

## Verification

- `python -m pytest tests/test_phase2.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real `results/splits.npz`, `results/class_distribution.csv`, and `results/figures/class_dist_across_splits.png` generation requires `results/dataset_cache.npz`.
