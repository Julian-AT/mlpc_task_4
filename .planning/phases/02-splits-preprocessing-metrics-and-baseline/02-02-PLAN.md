---
phase: 02-splits-preprocessing-metrics-and-baseline
plan: 02-02
name: Implement preprocessing, temporal context, and agreement helpers
status: planned
requirements: [PREP-01, PREP-02, PREP-03]
files: [src/config.py, src/preprocess.py, tests/test_phase2.py]
---

# Plan 02-02: Preprocessing and Agreement Helpers

## Objective

Implement train-only standardization, temporal-context concatenation, and high-agreement helpers for later model experiments.

## Tasks

1. Implement `fit_scaler`, `apply_scaler`, and split-aware scaling.
2. Implement temporal context with zero-padding at file boundaries.
3. Implement per-file/per-class pairwise IoU and high-agreement mask helpers.
4. Add synthetic tests proving scaling and temporal boundary behavior.

## Verification

- `python -m pytest tests/test_phase2.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real `results/preprocessed.npz`, `results/scaler.joblib`, and `results/per_file_iou.npz` generation requires `results/dataset_cache.npz` and `results/splits.npz`.
