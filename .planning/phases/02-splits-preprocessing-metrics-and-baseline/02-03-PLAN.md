---
phase: 02-splits-preprocessing-metrics-and-baseline
plan: 02-03
name: Implement metrics and class-prior baseline
status: planned
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
files: [src/metrics.py, src/baseline.py, tests/test_phase2.py]
---

# Plan 02-03: Metrics and Baseline

## Objective

Implement reusable multi-label metrics and the class-prior baseline expected by the Task 4 report.

## Tasks

1. Implement per-class AP, macro AP, micro AP, and optimal F1 threshold helpers.
2. Implement class-prior constant-score baseline.
3. Implement baseline JSON writer.
4. Add synthetic tests for AP/F1 behavior and constant-score baseline AP.

## Verification

- `python -m pytest tests/test_phase2.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real `results/baseline.json` generation requires `results/dataset_cache.npz` and `results/splits.npz`.
