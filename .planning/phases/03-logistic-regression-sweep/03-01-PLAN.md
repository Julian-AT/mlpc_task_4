---
phase: 03-logistic-regression-sweep
plan: 03-01
name: Implement and run the LR sweep with best-model persistence
status: planned
requirements: [LR-01, LR-02, LR-03]
files: [src/config.py, src/train_lr.py, tests/test_train_lr.py]
---

# Plan 03-01: LR Sweep and Best Model

## Objective

Implement one-vs-rest logistic regression training, grid sweep, best-model selection, and prediction archive writing.

## Tasks

1. Add LR output path constants.
2. Implement preprocessed cache loading, `fit_one`, grid iteration, and `sweep_lr`.
3. Save `results/lr_sweep.csv`, `results/lr_best.pkl`, and LR validation/test scores.
4. Add synthetic tests for fitting and sweep output files.

## Verification

- `python -m pytest tests/test_train_lr.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

The real LR sweep cannot run until `results/preprocessed.npz` exists.
