---
phase: 03-logistic-regression-sweep
plan: 03-02
name: Generate LR sweep figures and optional ablation notes
status: planned
requirements: [LR-04, LR-05]
files: [src/train_lr.py, tests/test_train_lr.py]
---

# Plan 03-02: LR Sweep Figure and Ablation Notes

## Objective

Implement the LR sweep visualization and explicitly defer optional ablations until real data is available.

## Tasks

1. Implement `plot_lr_sweep`.
2. Add a synthetic figure-writing test.
3. Record PCA/high-agreement ablations as skipped unless time remains after the real LR sweep.

## Verification

- `python -m pytest tests/test_train_lr.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

The real LR figure cannot be generated until `results/lr_sweep.csv` exists from the real sweep.
