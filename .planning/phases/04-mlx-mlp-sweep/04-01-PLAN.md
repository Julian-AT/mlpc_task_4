---
phase: 04-mlx-mlp-sweep
plan: 04-01
name: Implement MLX model, loss, training loop, and prediction utilities
status: planned
requirements: [MLP-01, MLP-02]
files: [src/config.py, src/train_mlp.py, tests/test_train_mlp.py]
---

# Plan 04-01: MLX Model and Training Loop

## Objective

Implement the MLX MLP model, weighted BCE loss, AdamW training loop, validation prediction, and early stopping.

## Verification

- `python -m pytest tests/test_train_mlp.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real training requires `results/preprocessed.npz`.
