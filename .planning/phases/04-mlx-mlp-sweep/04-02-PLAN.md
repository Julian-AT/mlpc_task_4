---
phase: 04-mlx-mlp-sweep
plan: 04-02
name: Run MLP sweep with early stopping and best-state persistence
status: planned
requirements: [MLP-03, MLP-04]
files: [src/config.py, src/train_mlp.py, tests/test_train_mlp.py]
---

# Plan 04-02: MLP Sweep and Best State

## Objective

Implement grid iteration, sweep CSV writing, best-weight persistence, and MLP prediction archive writing.

## Verification

- `python -m pytest tests/test_train_mlp.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real `results/mlp_sweep.csv`, `results/mlp_best.npz`, and MLP predictions require `results/preprocessed.npz`.
