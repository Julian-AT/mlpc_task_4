---
phase: 04-mlx-mlp-sweep
plan: 04-03
name: Generate MLP sweep figures, training curves, and ablation notes
status: planned
requirements: [MLP-05, MLP-06]
files: [src/train_mlp.py, tests/test_train_mlp.py]
---

# Plan 04-03: MLP Figure/Ablation Notes

## Objective

Capture training history during MLP runs so real training curves can be generated later, and defer optional ablations until the real sweep is available.

## Verification

- `python -m pytest tests/test_train_mlp.py -q --tb=short`
- `python -m pytest -q --tb=short`

## Dataset Gate

Real figures and ablation notes require real sweep results.
